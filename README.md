# ToDo-Django

### Build first Django project

- How to create a project in Python Django
  ```
  django-admin startproject todo . # create a project
  ```
- What is a app in Django
  ```
  python manage.py startapp user # create a app
  ```
- Request-Response Life cycle in Django
  ```
  python manage.py runserver # run the server
  ```
- File structure of Django

### How to write HTML in Django

- How to render in Django Template Language
- How to place data in HTML
- Variables
- Tags
- Filters
